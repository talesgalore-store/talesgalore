<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Odd One Out — TalesGalore Literary Games</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400;1,600&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
<style>
:root{--cream:#FDFAF4;--warm:#F5EDD8;--gold:#C8923A;--gold-lt:#E8B96A;--ink:#8B5E3C;--muted:#7A6A52;--rule:#E2D5BC;--dark:#3D2B0E;--success:#4A7C59;--danger:#B03030;--purple:#4A2C6B}
*{box-sizing:border-box;margin:0;padding:0}
body{background:var(--cream);font-family:'Lato',sans-serif;color:var(--dark);min-height:100vh}
.tg-header{background:var(--ink);padding:16px 28px;display:flex;align-items:center;gap:14px;box-shadow:0 2px 10px rgba(61,43,14,.2)}
.badge{background:var(--gold);color:#fff;font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;padding:3px 8px;border-radius:3px}
.tg-header h1{font-family:'Playfair Display',serif;font-size:24px;font-weight:700;color:#fff}
.tg-header h1 span{color:var(--gold-lt)}
.tg-header a.back{margin-left:auto;color:var(--rule);text-decoration:none;font-size:13px}
.tg-header a.back:hover{color:var(--gold-lt)}

.game-wrap{max-width:700px;margin:0 auto;padding:36px 20px 60px}

.start-screen{text-align:center;padding:36px 0}
.game-icon{font-size:72px;display:block;margin-bottom:18px;animation:waddle 2s ease-in-out infinite}
@keyframes waddle{0%,100%{transform:rotate(0deg)}25%{transform:rotate(-12deg)}75%{transform:rotate(12deg)}}
.start-screen h2{font-family:'Playfair Display',serif;font-size:32px;font-weight:700;color:var(--dark);margin-bottom:12px}
.start-screen p{color:var(--muted);font-size:16px;line-height:1.7;max-width:440px;margin:0 auto 26px}
.rules{background:var(--warm);border:1.5px solid var(--rule);border-radius:12px;padding:18px 22px;text-align:left;margin-bottom:26px}
.rules h4{font-family:'Playfair Display',serif;font-size:15px;color:var(--dark);margin-bottom:8px}
.rules li{color:var(--muted);font-size:14px;margin-left:18px;margin-bottom:5px}
.btn-primary{background:var(--purple);color:#fff;font-weight:700;font-size:16px;padding:13px 34px;border:none;border-radius:10px;cursor:pointer;transition:background .2s,transform .15s}
.btn-primary:hover{background:#6C3483;transform:scale(1.03)}

.progress-info{display:flex;justify-content:space-between;font-size:13px;color:var(--muted);margin-bottom:6px}
.progress-bar{background:var(--rule);border-radius:10px;height:7px;margin-bottom:20px;overflow:hidden}
.progress-fill{background:linear-gradient(90deg,var(--purple),#8E44AD);height:100%;border-radius:10px;transition:width .4s}
.timer-row{display:flex;align-items:center;gap:10px;margin-bottom:22px}
.timer-track{flex:1;background:var(--rule);border-radius:10px;height:10px;overflow:hidden}
.timer-fill{height:100%;border-radius:10px;background:var(--success);transition:width 1s linear,background .5s}

/* CATEGORY BANNER */
.cat-banner{
  background:var(--purple);color:#fff;
  border-radius:12px;padding:18px 22px;text-align:center;margin-bottom:22px;
}
.cat-label{font-size:11px;font-weight:700;letter-spacing:2px;text-transform:uppercase;opacity:.7;margin-bottom:6px}
.cat-name{font-family:'Playfair Display',serif;font-size:22px;font-weight:700}
.cat-sub{font-size:13px;margin-top:6px;opacity:.8;font-style:italic}

/* BOOK CARDS */
.books-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:20px}
.book-card{
  background:#fff;border:2px solid var(--rule);border-radius:14px;
  padding:20px 16px;cursor:pointer;text-align:center;
  transition:border-color .2s,background .2s,transform .2s;
  position:relative;
}
.book-card:hover:not(:disabled):not(.revealed){border-color:var(--purple);background:var(--warm);transform:translateY(-3px)}
.book-card .book-emoji{font-size:36px;display:block;margin-bottom:10px}
.book-card .book-title{font-family:'Playfair Display',serif;font-size:14px;font-weight:600;color:var(--dark);line-height:1.4}
.book-card .book-author{font-size:12px;color:var(--muted);margin-top:4px}

.book-card.correct-card{border-color:var(--success)!important;background:#EAF4EE!important;transform:none}
.book-card.wrong-card{border-color:var(--danger)!important;background:#FDECEA!important;transform:none}
.book-card.odd-revealed{border-color:var(--purple)!important;background:#F3EAF8!important;transform:none}

.badge-pill{
  position:absolute;top:-8px;right:10px;
  font-size:11px;font-weight:700;padding:3px 10px;border-radius:10px;
  display:none;
}
.badge-pill.show{display:block}
.badge-pill.odd{background:var(--purple);color:#fff}
.badge-pill.ok{background:var(--success);color:#fff}

.feedback{padding:14px 18px;border-radius:10px;font-size:14px;margin-top:4px;display:none;line-height:1.6}
.feedback.show{display:block}
.feedback.correct-fb{background:#EAF4EE;border:1.5px solid var(--success);color:#2E6645}
.feedback.wrong-fb{background:#FDECEA;border:1.5px solid var(--danger);color:#8B2020}
.next-btn{display:none;background:var(--purple);color:#fff;font-weight:700;font-size:14px;padding:11px 26px;border:none;border-radius:8px;cursor:pointer;margin-top:14px;transition:background .2s}
.next-btn.show{display:inline-block}
.next-btn:hover{background:#6C3483}

.score-screen{text-align:center;padding:20px 0}
.score-circle{width:130px;height:130px;border-radius:50%;background:var(--warm);border:4px solid var(--purple);display:flex;flex-direction:column;align-items:center;justify-content:center;margin:0 auto 22px;font-family:'Playfair Display',serif}
.score-circle .big{font-size:38px;font-weight:700;color:var(--dark);line-height:1}
.score-circle .out{font-size:14px;color:var(--muted)}
.score-screen h2{font-family:'Playfair Display',serif;font-size:28px;color:var(--dark);margin-bottom:10px}
.score-screen p{color:var(--muted);font-size:15px;max-width:420px;margin:0 auto 26px;line-height:1.7}
.btn-secondary{background:transparent;border:2px solid var(--gold);color:var(--gold);font-weight:700;font-size:14px;padding:10px 22px;border-radius:8px;cursor:pointer;margin:0 6px;transition:all .2s}
.btn-secondary:hover{background:var(--gold);color:#fff}
.tg-footer{background:var(--dark);color:var(--rule);text-align:center;padding:26px 20px;font-size:13px}
.tg-footer em{color:var(--gold);font-style:italic;font-family:'Playfair Display',serif;display:block;margin-bottom:5px}
@media(max-width:500px){.books-grid{grid-template-columns:1fr}.game-wrap{padding:22px 14px 48px}}
</style>
</head>
<body>
<header class="tg-header">
  <span class="badge">Games</span>
  <h1>Tales<span>Galore</span></h1>
  <a class="back" href="literary-games.html">← All Games</a>
</header>

<div class="game-wrap">
  <div id="startScreen" class="start-screen">
    <span class="game-icon">🦆</span>
    <h2>Odd One Out</h2>
    <p>Three of the four books belong to the same category. One sneaky imposter doesn't fit. Can you spot it every time?</p>
    <div class="rules">
      <h4>📋 How to play</h4>
      <ul>
        <li>Read the category name carefully</li>
        <li>Three books belong — one doesn't</li>
        <li>Click the book you think is the odd one out</li>
        <li>15 seconds per round — think fast!</li>
      </ul>
    </div>
    <button class="btn-primary" onclick="startGame()">Find the Imposter →</button>
  </div>

  <div id="gameScreen" style="display:none">
    <div class="progress-info">
      <span id="roundLabel">Round 1 of 8</span>
      <span id="scoreLabel">Score: 0</span>
    </div>
    <div class="progress-bar"><div class="progress-fill" id="progressFill" style="width:0%"></div></div>
    <div class="timer-row">
      <span>⏱</span>
      <div class="timer-track"><div class="timer-fill" id="timerFill" style="width:100%"></div></div>
      <span id="timerNum" style="font-weight:700;font-size:14px;min-width:26px;text-align:right">15</span>
    </div>

    <div class="cat-banner">
      <div class="cat-label">Category</div>
      <div class="cat-name" id="catName"></div>
      <div class="cat-sub" id="catSub"></div>
    </div>

    <div class="books-grid" id="booksGrid"></div>
    <div class="feedback" id="feedback"></div>
    <button class="next-btn" id="nextBtn" onclick="nextRound()">Next Round →</button>
  </div>

  <div id="scoreScreen" style="display:none" class="score-screen">
    <div class="score-circle">
      <span class="big" id="finalScore">0</span>
      <span class="out">/ 8</span>
    </div>
    <h2 id="resultTitle">Well spotted!</h2>
    <p id="resultMsg"></p>
    <button class="btn-primary" onclick="restartGame()">Play Again</button>
    <button class="btn-secondary" onclick="location.href='literary-games.html'">All Games</button>
  </div>
</div>

<footer class="tg-footer">
  <em>— TalesGalore Literary Games —</em>
  Stories worth rediscovering
</footer>

<script>
const rounds=[
  {
    cat:"Books by Roald Dahl",
    sub:"Three are Roald Dahl — one is not",
    books:[
      {title:"Fantastic Mr Fox",author:"Roald Dahl",emoji:"🦊",isOdd:false},
      {title:"The BFG",author:"Roald Dahl",emoji:"👨",isOdd:false},
      {title:"Charlie and the Great Glass Elevator",author:"Roald Dahl",emoji:"🛗",isOdd:false},
      {title:"The Tiger Who Came to Tea",author:"Judith Kerr",emoji:"🐯",isOdd:true},
    ],
    explain:"The Tiger Who Came to Tea was written by Judith Kerr, not Roald Dahl — it's one of the most beloved picture books from a completely different author!"
  },
  {
    cat:"Books featuring a Bear",
    sub:"Three have a bear as a key character — one doesn't",
    books:[
      {title:"Paddington Bear",author:"Michael Bond",emoji:"🐻",isOdd:false},
      {title:"We're Going on a Bear Hunt",author:"Michael Rosen",emoji:"🏕️",isOdd:false},
      {title:"Winnie-the-Pooh",author:"A.A. Milne",emoji:"🍯",isOdd:false},
      {title:"The Jungle Book",author:"Rudyard Kipling",emoji:"🐆",isOdd:true},
    ],
    explain:"The Jungle Book features Baloo the bear, but the story's central character is Mowgli the boy — whereas the other three books literally star their bears!"
  },
  {
    cat:"Books set in Magical Worlds",
    sub:"Three feature entirely magical or fantasy worlds",
    books:[
      {title:"The Lion, the Witch and the Wardrobe",author:"C.S. Lewis",emoji:"❄️",isOdd:false},
      {title:"Alice's Adventures in Wonderland",author:"Lewis Carroll",emoji:"🃏",isOdd:false},
      {title:"The Wizard of Oz",author:"L. Frank Baum",emoji:"🌈",isOdd:false},
      {title:"Matilda",author:"Roald Dahl",emoji:"📚",isOdd:true},
    ],
    explain:"Matilda is set in the very real world of a real school and a real village — even Matilda's telekinesis is presented as a hidden talent, not a journey to a fantasy realm."
  },
  {
    cat:"Julia Donaldson Books",
    sub:"Three are by Julia Donaldson — one is not",
    books:[
      {title:"The Gruffalo",author:"Julia Donaldson",emoji:"👹",isOdd:false},
      {title:"Room on the Broom",author:"Julia Donaldson",emoji:"🧹",isOdd:false},
      {title:"Zog",author:"Julia Donaldson",emoji:"🐉",isOdd:false},
      {title:"Elmer the Elephant",author:"David McKee",emoji:"🐘",isOdd:true},
    ],
    explain:"Elmer the patchwork elephant was created by David McKee, first published in 1989 — predating Julia Donaldson's picture book career!"
  },
  {
    cat:"Books with a Dragon or Monster",
    sub:"Three feature a dragon or monster as a key character",
    books:[
      {title:"The Enormous Crocodile",author:"Roald Dahl",emoji:"🐊",isOdd:true},
      {title:"How to Train Your Dragon",author:"Cressida Cowell",emoji:"🐲",isOdd:false},
      {title:"The Gruffalo",author:"Julia Donaldson",emoji:"👹",isOdd:false},
      {title:"Dave's Christmas Cracker",author:"Rory Tyger",emoji:"🎄",isOdd:false},
    ],
    explain:"The Enormous Crocodile features a very nasty crocodile — a real animal — not a dragon or invented monster like the Gruffalo, Dave the dragon, or the How to Train Your Dragon creatures!"
  },
  {
    cat:"Bedtime / Goodnight Stories",
    sub:"Three are classic bedtime or goodnight books",
    books:[
      {title:"Goodnight Moon",author:"Margaret Wise Brown",emoji:"🌙",isOdd:false},
      {title:"Time for Bed",author:"Mem Fox",emoji:"😴",isOdd:false},
      {title:"Peace at Last",author:"Jill Murphy",emoji:"🐻",isOdd:false},
      {title:"The Very Hungry Caterpillar",author:"Eric Carle",emoji:"🐛",isOdd:true},
    ],
    explain:"The Very Hungry Caterpillar is a book about days of the week, counting, and transformation — it's a daytime learning adventure, not a bedtime story!"
  },
  {
    cat:"Books with a Magic School",
    sub:"Three feature a school with magical or unusual elements",
    books:[
      {title:"Harry Potter and the Philosopher's Stone",author:"J.K. Rowling",emoji:"🪄",isOdd:false},
      {title:"The Worst Witch",author:"Jill Murphy",emoji:"🧙‍♀️",isOdd:false},
      {title:"Mildred Hubble",author:"Jill Murphy",emoji:"🎃",isOdd:true},
      {title:"A Wizard of Earthsea",author:"Ursula K. Le Guin",emoji:"🌊",isOdd:false},
    ],
    explain:"Mildred Hubble is actually the main character IN The Worst Witch — she's not a separate book! The other three are all distinct novels featuring magical schools."
  },
  {
    cat:"Classic Fairy Tales",
    sub:"Three are traditional fairy tales — one is a modern creation",
    books:[
      {title:"Cinderella",author:"Charles Perrault",emoji:"👗",isOdd:false},
      {title:"Hansel and Gretel",author:"Brothers Grimm",emoji:"🏠",isOdd:false},
      {title:"Sleeping Beauty",author:"Brothers Grimm",emoji:"💤",isOdd:false},
      {title:"The Fairytale Hairdresser",author:"Abie Longstaff",emoji:"✂️",isOdd:true},
    ],
    explain:"The Fairytale Hairdresser is a delightful modern series by Abie Longstaff — a contemporary creation that plays with fairy tale characters, not an original traditional tale!"
  },
];

let current=0,score=0,timer=null,timeLeft=15;

function startGame(){
  document.getElementById('startScreen').style.display='none';
  document.getElementById('gameScreen').style.display='block';
  showRound();
}

function shuffle(a){return [...a].sort(()=>Math.random()-.5)}

function showRound(){
  const r=rounds[current];
  document.getElementById('roundLabel').textContent=`Round ${current+1} of ${rounds.length}`;
  document.getElementById('scoreLabel').textContent=`Score: ${score}`;
  document.getElementById('progressFill').style.width=`${(current/rounds.length)*100}%`;
  document.getElementById('catName').textContent=r.cat;
  document.getElementById('catSub').textContent=r.sub;
  document.getElementById('feedback').className='feedback';
  document.getElementById('nextBtn').className='next-btn';

  const shuffled=shuffle(r.books);
  const grid=document.getElementById('booksGrid');
  grid.innerHTML='';
  shuffled.forEach((b,i)=>{
    const card=document.createElement('div');
    card.className='book-card';
    card.innerHTML=`
      <span class="badge-pill ${b.isOdd?'odd':'ok'}" id="badge${i}">
        ${b.isOdd?'Odd One Out ✓':'Belongs ✓'}
      </span>
      <span class="book-emoji">${b.emoji}</span>
      <div class="book-title">${b.title}</div>
      <div class="book-author">by ${b.author}</div>
    `;
    card.onclick=()=>pick(b.isOdd,card,i,shuffled);
    grid.appendChild(card);
  });

  timeLeft=15;
  clearInterval(timer);
  updateTimerUI();
  timer=setInterval(()=>{
    timeLeft--;
    updateTimerUI();
    if(timeLeft<=0){clearInterval(timer);timeOut(shuffled);}
  },1000);
}

function pick(isOdd,card,idx,shuffled){
  clearInterval(timer);
  const cards=document.querySelectorAll('.book-card');
  cards.forEach(c=>c.onclick=null);
  const fb=document.getElementById('feedback');

  if(isOdd){
    card.classList.add('correct-card');
    card.querySelector('.badge-pill').classList.add('show');
    score++;
  } else {
    card.classList.add('wrong-card');
    cards.forEach((c,i)=>{
      if(shuffled[i].isOdd){
        c.classList.add('odd-revealed');
        c.querySelector('.badge-pill').classList.add('show');
      }
    });
    fb.textContent=`❌ Not quite! ${rounds[current].explain}`;
    fb.className='feedback show wrong-fb';
    document.getElementById('scoreLabel').textContent=`Score: ${score}`;
    document.getElementById('nextBtn').className='next-btn show';
    return;
  }
  fb.textContent=`✅ Correct! ${rounds[current].explain}`;
  fb.className='feedback show correct-fb';
  document.getElementById('scoreLabel').textContent=`Score: ${score}`;
  document.getElementById('nextBtn').className='next-btn show';
}

function timeOut(shuffled){
  const cards=document.querySelectorAll('.book-card');
  cards.forEach((c,i)=>{
    c.onclick=null;
    if(shuffled[i].isOdd){
      c.classList.add('odd-revealed');
      c.querySelector('.badge-pill').classList.add('show');
    }
  });
  const fb=document.getElementById('feedback');
  fb.textContent=`⏰ Time's up! ${rounds[current].explain}`;
  fb.className='feedback show wrong-fb';
  document.getElementById('nextBtn').className='next-btn show';
}

function updateTimerUI(){
  const pct=(timeLeft/15)*100;
  const fill=document.getElementById('timerFill');
  fill.style.width=pct+'%';
  fill.style.background=timeLeft>8?'#4A7C59':timeLeft>4?'var(--gold)':'var(--danger)';
  document.getElementById('timerNum').textContent=timeLeft;
}

function nextRound(){
  current++;
  if(current>=rounds.length) showResult();
  else showRound();
}

function showResult(){
  clearInterval(timer);
  document.getElementById('gameScreen').style.display='none';
  document.getElementById('scoreScreen').style.display='block';
  document.getElementById('finalScore').textContent=score;
  const pct=score/rounds.length;
  let title,msg;
  if(pct===1){title='🏆 Master Spotter!';msg='Perfect! Every imposter identified — your literary knowledge is extraordinary!';}
  else if(pct>=.75){title='⭐ Sharp Eyes!';msg='Brilliant work! You spotted most of the odd ones out with ease.';}
  else if(pct>=.5){title='📚 Good Instincts!';msg='Solid performance! A few tricky categories in there — try again and you might get them all!';}
  else{title='🌱 Keep Exploring!';msg='Every round teaches you something new about books. Come browse TalesGalore and build up that knowledge!';}
  document.getElementById('resultTitle').textContent=title;
  document.getElementById('resultMsg').textContent=msg;
}

function restartGame(){
  current=0;score=0;
  document.getElementById('scoreScreen').style.display='none';
  document.getElementById('gameScreen').style.display='block';
  showRound();
}
</script>
</body>
</html>
